import { StyleSheet } from "react-native";

export const AppStyles = StyleSheet.create({
    screenView: {
        flex: 1,
        backgroundColor: '#272643',
        padding: 8,
    },
    text: {
        color: 'white'
    }
});
